server:
  port: 8010
spring:
  application:
    name: pavis-upms-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  redis:
    host: localhost
    port: 6379
    database: 0
  datasource:
    url: jdbc:mysql://localhost:3306/upms?useUnicode=true&characterEncoding=utf-8
    username: root
    password: Semi.1001
mybatis-plus:
  mapper-locations: classpath:/mapper/*Mapper.xml
  typeAliasesPackage: com.pavis.upmsservice.model
  global-config:
    db-config:
      id-type: auto